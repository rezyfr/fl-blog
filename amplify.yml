version: 1
frontend:
  phases:
  build:
    commands: flutter build web --release
  postBuild:
    commands: cd build
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: web
    files:
      - '**/*'
  cache:
    paths: []
